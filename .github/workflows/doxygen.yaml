on:
  push:
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: mattnotmitt/doxygen-action@v1.9.5
        with:
          enable-latex: true
